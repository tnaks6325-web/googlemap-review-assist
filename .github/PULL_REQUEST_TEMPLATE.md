## 변경 목적

<!-- 이 PR이 해결하는 운영/기능 문제를 간단히 적습니다. -->

## 검증

- [ ] `npm test`
- [ ] `npm run build`
- [ ] Vercel Preview 확인 (해당하는 경우)

## 운영 배포 계보

- 대상 브랜치: `main`
- CI 실행 URL:
- 병합 후 `main` commit SHA:
- Vercel Production URL:
- `/api/health` 결과:

## 롤백 계획

<!-- 문제가 생기면 이 PR의 병합 commit을 revert하는 PR을 만듭니다. -->
