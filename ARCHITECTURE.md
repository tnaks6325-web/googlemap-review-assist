# 리뷰 작성 보조 플랫폼 — 기획·아키텍처 설계

> 본 문서는 **기획·아키텍처**만 다룬다. 코드는 포함하지 않는다.
> 스택: **Next.js 풀스택 + PostgreSQL**

---

## 0. Context (왜 이 설계인가)

소상공인의 구글맵 활성화를 돕기 위해, **진짜 방문 고객이 리뷰를 손쉽게 남기도록 돕는** 보조 플랫폼을 만든다.

원래 기획(AI가 리뷰 내용을 창작 → 대가성 적립금 지급 → 하루 20건 할당 → 타 리뷰 스크래핑으로 내용 합성)은 구글·네이버 정책과 「표시·광고의 공정화에 관한 법률」을 정면으로 위반하는 **대가성 가짜 리뷰(astroturfing)** 시스템이다. 적발 시 의뢰 업체가 순위 강등·리뷰 삭제·계정 정지로 더 큰 피해를 본다.

따라서 같은 목표("리뷰 작성의 번거로움 제거")를 **합법적으로** 달성하는 구조로 재설계한다.

| 원래 기획 (위반) | 재설계 (합법) |
|---|---|
| AI가 리뷰 내용을 창작 | 고객 본인의 답변을 AI가 **문장으로 다듬는 초안**만 제공 |
| 리뷰 게시에 적립금 지급 | 적립금은 **비공개 피드백 제출**에 지급, 공개 리뷰와 분리 |
| 타 리뷰/네이버 스크래핑으로 내용 합성 | 고객 답변이 유일한 사실 출처. 업체 정보(메뉴명 등)는 표현 보조용 경량 컨텍스트로만 |
| 하루 20건 할당량 | 할당량 없음. 실제 방문 건수가 상한 |

---

## 1. 제품 개념 & 사용자 플로우

### 1.1 고객(리뷰어) 플로우 — 모바일 웹, QR/링크 진입
매장에 비치된 QR 또는 영수증 링크로 진입. 별도 앱 설치 없음.

```
QR 스캔
  └─ ① 영수증 인증 (사진 업로드 또는 코드 입력) → 방문 사실 검증
       └─ ② 비공개 설문 (클릭형: 별점 + 좋았던 메뉴 선택 + 한 줄 소감)
            └─ [여기서 적립금 적립 확정] ← 공개 게시와 무관
                 └─ ③ AI 초안 제공 — 고객이 ②에서 입력한 내용을 자연스러운 문장으로 다듬음
                      └─ ④ (선택·자율) 고객이 직접 구글맵에 복사·게시
                           · 초안 편집 자유, 게시 여부 자유
                           · 대가 표시 문구는 정책상 필요 시 자동 부기
```

핵심: **②까지 완료하면 보상은 끝.** ③④는 "쓰기 편하게 도와주는" 부가 가치이며 보상과 연결되지 않는다.
→ 클릭만으로 끝나는 UX 목표는 유지하되, 게시 강제·게시 보상은 없음.

### 1.2 사장님(광고주) 플로우 — 웹 대시보드
```
가입/매장 등록 (구글맵 Place 연결, 메뉴 등록)
  └─ 리뷰 요청 채널 발급 (매장 QR / 영수증용 단축링크)
       └─ 대시보드: 비공개 피드백 통계(별점 추이·인기 메뉴·불만 키워드)
            · 공개 리뷰 게시 전환율(참고 지표)
            · 적립금 사용 현황 / 정산 내역
       └─ 구독료 결제 (플랫폼 수익 모델)
```

---

## 2. 컴플라이언스 설계 (가장 중요)

본 설계의 합법성은 아래 불변 조건(invariants)에 의존한다. 구현 전반에서 강제되어야 한다.

1. **보상 트리거 = 비공개 피드백 제출.** 적립금 지급 이벤트는 `feedback.submitted`에만 연결한다. `review.posted`(공개 게시)에는 어떤 보상도 연결하지 않는다. 코드/DB 레벨에서 `review.posted`가 적립 트랜잭션을 생성하지 못하도록 차단.
2. **AI는 창작하지 않는다.** AI 초안의 사실 입력은 고객이 ②에서 제출한 답변뿐. 메뉴명·업체명 등 업체 정보는 *표현 정확도* 보조로만 사용하고, 경험/감상을 지어내지 않는다. (타 리뷰·네이버 리뷰를 긁어 내용을 합성하는 기능은 설계에서 제외)
3. **게시는 자율.** 게시 버튼·강제 없음. 초안은 클립보드 복사 + 구글맵 딥링크 안내 수준. 고객이 자유롭게 수정/폐기 가능.
4. **대가 표시.** 향후 어떤 형태로든 공개 게시와 보상이 근접하는 기능을 검토한다면, 「추천·보증 등에 관한 표시·광고 심사지침」에 따른 경제적 이해관계 표시 문구를 자동 삽입해야 한다. (현재 설계에서는 분리되어 있어 원칙적으로 불필요)
5. **할당량 없음.** 매장당 인위적 일일 목표치(20건 등) 미구현. 적립은 1방문(영수증) 1회로 제한해 영수증 재사용·자동화 어뷰징 차단.

> 이 5개 조건을 깨는 기능 요청은 다시 위반 영역으로 들어가므로, 구현 단계에서 재확인 필요.

---

## 3. 데이터 모델 (PostgreSQL, 개념 스키마)

```
businesses        매장 (구글 Place ID, 상호, 주소, 구독상태)
  └─ menus        매장 메뉴 (이름, 카테고리) — AI 표현 보조 컨텍스트
  └─ campaigns    리뷰 요청 채널 (QR/단축링크, 활성상태)

reviewers         고객 계정 (휴대폰 인증 기반, 최소 정보)
  └─ point_wallet 적립금 지갑 (잔액)

receipts          영수증 인증 (이미지/코드, 검증상태, 금액·일시, 중복방지 해시)
                  ↑ business_id, reviewer_id

feedbacks         비공개 설문 응답 (별점, 선택메뉴[], 한줄소감)
                  ↑ receipt_id (1:1) — 보상 트리거의 단위
  └─ ai_drafts    AI가 생성한 리뷰 초안 (입력=feedback, 버전 이력)

point_transactions  적립/차감/정산 원장 (append-only)
                    type: EARN(=feedback.submitted) | REDEEM | SETTLE | ADJUST
                    idempotency_key (영수증당 1 EARN 보장)

settlements       정산 요청/지급 내역 (지급수단, 상태, 처리일)

owners            사장님 계정 / businesses와 연결
subscriptions     사장님 구독·결제 (플랫폼 수익)
```

무결성 포인트:
- `receipts.dedupe_hash` 유니크 → 동일 영수증 재사용 차단.
- `point_transactions`는 **append-only 원장**(이중지급·잔액조작 방지). 잔액은 원장 합계로 도출하거나 검증.
- `EARN` 트랜잭션은 반드시 `feedback_id`를 참조하고 `review` 관련 엔티티는 참조 불가 (컴플라이언스 invariant #1을 스키마로 강제).

---

## 4. 시스템 아키텍처 (Next.js 풀스택)

```
[고객 모바일 웹]   [사장님 대시보드]
        │                │
        └──── Next.js (App Router) ────┘
              ├─ UI (React Server/Client Components)
              ├─ Route Handlers / Server Actions  ← API 계층
              │     ├─ /api/receipts        영수증 인증
              │     ├─ /api/feedback        설문 제출 + 적립(EARN) [트랜잭션]
              │     ├─ /api/drafts          AI 초안 생성
              │     ├─ /api/points          잔액/내역
              │     ├─ /api/settlements     정산 요청
              │     └─ /api/business/*      매장·메뉴·통계
              ├─ 인증: 고객=휴대폰 OTP, 사장님=이메일/비번 (NextAuth 등)
              └─ 도메인 서비스 계층 (point ledger, receipt verify, draft gen)
                        │
        ┌───────────────┼────────────────┬─────────────────┐
   PostgreSQL      Object Storage     LLM API          (선택) 결제 PG
   (Prisma ORM)   (영수증 이미지)   (Claude API)      (구독·정산)
```

- **AI 초안 생성**: Claude API. 프롬프트는 고객 답변(별점/메뉴/소감) + 매장 메뉴명을 입력으로 받아 "고객의 말을 자연스럽게 다듬는다"는 역할로 제약. 경험을 지어내지 말 것을 시스템 프롬프트에 명시. (모델: `claude-opus-4-8`, 비용 고려 시 `claude-haiku-4-5`)
- **영수증 인증**: MVP는 코드/번호 입력 + 사장님 발급 캠페인 매칭. 이미지 OCR 자동검증은 후속.
- **적립 처리**: `/api/feedback`에서 설문 저장과 `EARN` 원장 기록을 **단일 DB 트랜잭션**으로 묶고 `idempotency_key`(=receipt_id)로 중복 적립 차단.

### 제안 프로젝트 구조
```
/app                 라우트 (고객/사장님/api)
/lib
  /domain            point-ledger, receipt-verify, draft-service (순수 도메인 로직)
  /db                Prisma 스키마·클라이언트
  /ai                Claude 프롬프트·클라이언트
/components          UI
/prisma/schema.prisma
```

---

## 5. 적립금 누적·정산 설계 (핵심 기능)

- **누적(EARN)**: `feedback.submitted` 시 정책상 정해진 포인트를 원장에 적립. (게시 여부와 무관)
- **사용(REDEEM)**: 향후 제휴처/쿠폰 등에서 차감.
- **정산(SETTLE)**: 고객이 정산 요청 → 최소 정산액·본인확인 검증 → `settlements` 기록 + 원장 `SETTLE` 차감 → 지급수단 처리.
- **원장 원칙**: 모든 변동은 `point_transactions` append-only. 잔액 불일치 검출용 정합성 체크 잡 포함.
- **어뷰징 방지**: 영수증 1건 1적립, 휴대폰 인증, 비정상 패턴(동일 기기 다계정 등) 모니터링 훅 자리 마련.

---

## 6. MVP 범위 vs 후속

**MVP에 포함**: 매장/메뉴 등록, 캠페인(QR/링크) 발급, 영수증 코드 인증, 비공개 설문, EARN 적립, AI 초안 생성, 구글맵 딥링크 안내, 사장님 기본 통계, 적립금 잔액/내역.

**후속**: 영수증 이미지 OCR 자동검증, 정산 지급수단 연동(PG), 사장님 구독 결제, 관리자 백오피스, 고급 통계, 어뷰징 자동 탐지.

---

## 7. 설계 검증 / 다음 단계

1. **컴플라이언스 워크스루**: §2의 5개 invariant를 각 플로우(§1)와 스키마(§3)에 대조해 위반 경로가 없는지 확인.
2. **데이터 모델 리뷰**: `EARN`이 `feedback`만 참조하고 `review`를 참조할 수 없음을 스키마 제약으로 확인.
3. **사용자 흐름 검토**: ②까지로 보상이 종결되고 ③④가 자율임을 화면 흐름에서 확인.
4. **다음 단계**: 승인 시 이 아키텍처 기반의 구체 구현 계획(스키마 DDL, 라우트 명세, AI 프롬프트 초안) 작성.
